# Project Rules

## Deployment & Publishing Safety
- **Explicit Approval Required**: Never run `dart pub publish` or `flutter pub publish` (or any package deployment commands) automatically or in non-interactive mode (`-f` / `--force`).
- Always ask for explicit user confirmation before executing any deployment or publishing actions.
